# Directory where charset files should be installed
charsetsdir = $(datadir)/catdoc

# List of charset files to install
charsets_DATA = *.txt

# Make sure these files are included in the distribution
EXTRA_DIST = $(charsets_DATA)